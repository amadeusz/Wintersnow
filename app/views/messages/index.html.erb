<div id="wiadomosci">

<h1>Wiadomości</h1>

<table>
<% @messages.each do |message| %>
	<tr>
		<td style="width: 1px"><%= message.id %></td>
		<td><%= message.tresc %></td>
		<td style="width: 1px; white-space: nowrap"><%= message.data.to_s[2, message.data.to_s.length-11] %></td>
		<td style="width: 1px; white-space: nowrap"><%= link_to image_tag('icons/keyboard.png'), edit_message_path(message) %> <%= link_to image_tag('icons/destroy.png'), message, :confirm => 'Are you sure?', :method => :delete %></td>
	</tr>
<% end %>
</table>

	<div class="akcje"><%= link_to 'dodaj', new_message_path, :class => 'przycisk' %></div>
</div>
